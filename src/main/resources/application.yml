
spring:
  datasource:
    url: jdbc:mysql://lego-db-sf.mysql.database.azure.com:3306/lego?useSSL=true
    username: legoroot
    password: Piotrek123
  jpa:
    show-sql: true
    database: mysql
    database-platform: org.hibernate.dialect.MySQLDialect
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update

#server:
#  port: 9090
logging:
  level:
    com:
      zaxxer:
        hikari: DEBUG
